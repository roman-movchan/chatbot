framework:
    test: true
    session:
        storage_id: session.storage.mock_file

doctrine:
    dbal:
        url: '%env(resolve:DATA_DB_DRIVER)%://%env(resolve:DATA_DB_USER)%:%env(resolve:DATA_DB_PASS)%@%env(resolve:DATA_DB_HOST)%:3306/%env(resolve:DATA_DB_DATABASE)%_test'
